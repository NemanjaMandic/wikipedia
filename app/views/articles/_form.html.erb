<%= simple_form_for @article do |f| %>
  <%= f.collection_select :category_id, Category.all, :id, :name, { prompt: "Izaberi Kategoriju" } %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>